      SUBROUTINE(RESULT,VC.ID,BILL.ID,ATTR)
      CLIENT.ID=OCONV(BILL.ID,'TFB.BILLS.CRX;X;;1')
      RESULT=""
      IF CLIENT.ID#"" THEN
         VC.FNAME='VEND.CUST,':CLIENT.ID
*        REG=OCONV(DIV,'T':DIV.FNAME:';X;;45')
         RESULT=OCONV(VC.ID,'T':VC.FNAME:';X;;':ATTR)
      END
      RETURN
      END
