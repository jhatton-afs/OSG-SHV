      SUBROUTINE(RESULT,DIV,BILL.ID,ATTR)
      CLIENT.ID=OCONV(BILL.ID,'TFB.BILLS.CRX;X;;1')
      RESULT=""
      IF CLIENT.ID#"" THEN
         DIV.FNAME='DIVISION.CODES,':CLIENT.ID
*        REG=OCONV(DIV,'T':DIV.FNAME:';X;;45')
         RESULT=OCONV(DIV,'T':DIV.FNAME:';X;;':ATTR)
      END
      RETURN
      END
