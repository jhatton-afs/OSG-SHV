      SUBROUTINE(RESULT,DIV,BILL.ID,FLD)
      CLIENT.ID=OCONV(BILL.ID,'TFB.BILLS.CRX;X;;1')
      REG=""
      IF CLIENT.ID#"" THEN
         DIV.FNAME='DIVISION.CODES,':CLIENT.ID
         REG=OCONV(DIV,'T':DIV.FNAME:';X;;':FLD)
      END
      RESULT=REG
      RETURN
      END
