      OPEN 'FB.TABLES' TO F.TABLE ELSE STOP
      FLG = 0
      LOOP
         READV FLG FROM F.TABLE,'LOOPER',1 ELSE FLG = 1
      UNTIL FLG = 1 DO
         EXECUTE "INET.FB.ESTIMATOR.L"
         SLEEP 10
      REPEAT
