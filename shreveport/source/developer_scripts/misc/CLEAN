      OPEN 'CLEAN' TO F.CLEAN ELSE STOP
      EOJ=0
      EXECUTE "GET-LIST SCRACTH"
      LOOP
         READNEXT ID ELSE EOJ =1
      UNTIL EOJ DO
         WRITEV "" ON F.CLEAN,ID,1
      REPEAT
